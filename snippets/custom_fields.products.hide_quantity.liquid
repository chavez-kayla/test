{% if product.metafields.custom_fields["hide_quantity"] != blank %}
  <div class="custom-field custom-field__hide-quantity custom-field__type--checkbox">
    <h3 class="custom-field--title">Hide Quantity</h3>
    <div class="custom-field--value">
      {% if product.metafields.custom_fields["hide_quantity"] == 1 %}
        TRUE
      {% else %}
        FALSE
      {% endif %}
    </div>
  </div>
{% endif %}